# Godot 4 imports
.godot/
.import/

# OS / editor
.DS_Store
*.swp
*~

# Local config
export_credentials.cfg
godot
Godot_v4.3-stable_linux.x86_64
godot
godot.zip

# Godot 4.4+ script UID sidecars are committed (this project targets 4.6) — they
# keep script/resource references stable across renames. Not ignored.

# Local harness runtime state
.claude/scheduled_tasks.lock

# iOS / Android export output (generated by Godot + Xcode — not source)
/build/
/lemmings/
/lemmings.xcodeproj/
/lemmings.xcarchive/
/lemmings.xcframework/
/MoltenVK.xcframework/
/lemmings.ipa
/lemmings.pck
/Packaging.log
/DistributionSummary.plist
/ExportOptions.plist
/PrivacyInfo.xcprivacy
