# Музыка

Placeholder. Положите сюда `.ogg` треки:

- `menu_theme.ogg` — главное меню
- `level_theme.ogg` — игровой процесс

При отсутствии файла `AudioManager.play_music()` тихо пропускает воспроизведение.
